# UGC 面板识别

识别流程：四个白色方块定位 → 归一化到 1240×740 → 裁剪四组数值列 → 去除数值前冒号 → 数字 OCR → 合理范围校验 → 按 UGC 倍率解码。

定位框会将 1K、2.5K 等不同窗口分辨率统一映射到同一个安全区。高分辨率下更清晰的前导冒号会在 OCR 前按字符高度剔除，避免被误识别为 `1` 或 `2`；ATK 与白值超过四位数时会直接判定为识别异常。

当前数字 OCR 后端使用本地 Tesseract，不依赖 `pytesseract`。可通过环境变量 `GENSHIN_TESSERACT` 指定 `tesseract.exe`。
